웹 보안을 공부하기 전에 먼저 웹이 어떤 구조로 동작하는지 잡고 가는 이유는, 대부분의 웹 공격이 결국 **브라우저와 서버가 데이터를 주고받는 과정**에서 발생하기 때문이다. SQL Injection, XSS, CSRF, 쿠키 탈취 같은 공격도 전부 “브라우저가 요청을 보내고, 서버가 응답한다”는 기본 구조 위에서 이해해야 한다.
# Web

웹은 WWW, 즉 World Wide Web의 줄임말이고, 인터넷을 통해 서로 연결된 문서와 자원들의 시스템이다. 여기서 자원이라는 것은 단순한 HTML 문서만 의미하는 것이 아니라 웹 페이지, 이미지, 영상, 애플리케이션 같은 여러 데이터를 포함한다.

이 자원들은 각각 URL로 식별된다. 예를 들어 `https://example.com/index.html` 같은 주소가 특정 자원의 위치를 나타낸다. 사용자는 브라우저에 URL을 입력하거나 링크를 클릭하고, 브라우저는 그 URL을 기준으로 서버에 필요한 문서를 요청한다.

기본 흐름은 다음과 같다.

![](../images/Pasted%20image%2020260603223233.png)

- 사용자가 브라우저를 통해 URL을 입력한다.  
- 브라우저는 해당 URL에 맞는 웹 서버로 요청을 보낸다.  
- 웹 서버는 요청받은 문서를 찾아서 HTML 형태로 응답한다.  
- 브라우저는 서버가 보낸 HTML 파일을 해석해서 화면에 보여준다.

즉, 웹은 단순히 “페이지를 보는 것”이 아니라, **클라이언트인 브라우저가 서버에 요청하고, 서버가 그 요청에 대한 응답을 보내는 구조**라고 볼 수 있다.

여기서 중요한 점은 브라우저가 직접 서버의 내부 파일을 보는 것이 아니라는 점이다. 브라우저는 URL을 통해 “이 자원을 주세요”라고 요청하고, 서버는 허용된 범위 안에서 HTML 파일이나 이미지 같은 응답을 만들어 보내준다.

![](../images/Pasted%20image%2020260603223317.png)

그래서 웹 보안에서는 이 요청과 응답 사이를 계속 의심해야 한다. 사용자가 보낸 URL, 입력값, 쿠키, 폼 데이터가 안전한지 확인해야 하고, 서버가 응답으로 보내는 HTML이나 스크립트가 브라우저에서 어떻게 실행되는지도 봐야 한다. 결국 웹 보안은 이 기본적인 요청-응답 구조를 보호하는 것에서 시작된다.

---
# HTTP is Stateless

웹은 브라우저와 서버가 요청과 응답을 주고받는 구조인데, 여기서 바로 문제가 생긴다. HTTP는 기본적으로 **상태를 기억하지 않는 프로토콜**이다. 그래서 서버는 요청 하나하나를 독립적으로 처리하고, 이전에 같은 사용자가 어떤 요청을 했는지 자동으로 알지 못한다.

예를 들어 사용자가 `/login`에 접속한 다음 `/mypage`에 접속했다고 해도, 서버 입장에서는 그냥 두 개의 HTTP 요청이 따로 들어온 것처럼 보인다.

```text
GET /login HTTP/1.1
Host: example.com

GET /mypage HTTP/1.1
Host: example.com
```

이 두 요청이 같은 사용자의 요청인지, 로그인한 사용자의 요청인지 HTTP 자체만으로는 판단할 수 없다. 그래서 로그인 상태를 유지하기 위한 별도의 장치가 필요하다. 그 역할을 하는 대표적인 것이 **쿠키**다.

---
# Cookie

쿠키는 서버가 브라우저에게 저장하라고 보내는 작은 데이터이다. 브라우저는 쿠키를 저장해두었다가, 이후 같은 서버로 요청을 보낼 때 쿠키를 함께 보낸다. 그러면 서버는 쿠키 값을 보고 “이 사용자는 이전에 로그인한 사용자다”처럼 상태를 구분할 수 있다.

![](../images/Pasted%20image%2020260603224743.png)

즉, 쿠키는 HTTP의 stateless 특성을 보완하기 위해 사용된다. 서버가 모든 연결을 계속 붙잡고 있는 것이 아니라, 브라우저가 쿠키를 들고 다니면서 요청할 때마다 서버에게 자신을 식별할 수 있는 정보를 보내는 구조다.

여기서 중요한 점은 쿠키 안에 단순한 설정값만 들어가는 것이 아니라, **사용자 인증 토큰**이나 **세션 ID** 같은 중요한 정보가 들어갈 수 있다는 것이다. 그래서 쿠키가 탈취되면 공격자가 사용자의 로그인 상태를 그대로 흉내 낼 수 있다.

그래서 쿠키를 보호하기 위한 설정이 필요하다. 대표적으로 `Secure`, `HttpOnly`, `SameSite`가 있다.

---
# Protection of cookie

![](../images/Pasted%20image%2020260603225245.png)

`Secure`는 쿠키가 HTTPS 연결에서만 전송되도록 하는 설정이다. 이 설정이 있으면 브라우저는 HTTP처럼 암호화되지 않은 연결에서는 쿠키를 보내지 않는다. 즉, 네트워크 중간에서 쿠키가 노출될 가능성을 줄여준다.

`HttpOnly`는 JavaScript가 쿠키에 접근하지 못하도록 막는 설정이다. 원래 브라우저 안의 JavaScript는 `document.cookie` 같은 방식으로 쿠키를 읽을 수 있다. 그런데 XSS 공격이 발생하면 공격자가 삽입한 JavaScript가 사용자의 쿠키를 읽어갈 수 있다. `HttpOnly`를 설정하면 JavaScript에서는 쿠키를 읽을 수 없고, 브라우저가 서버로 요청을 보낼 때만 자동으로 쿠키를 붙여 보낸다.

`SameSite`는 다른 사이트에서 시작된 요청에 쿠키를 보낼지 제한하는 설정이다. 이 설정은 CSRF 공격을 막는 데 중요하다. CSRF는 사용자가 이미 로그인된 상태를 이용해서, 공격자가 다른 사이트에서 원래 사이트로 요청을 보내게 만드는 공격이다. `SameSite=Lax`나 `SameSite=Strict`를 설정하면 이런 외부 요청에 쿠키가 자동으로 붙는 상황을 줄일 수 있다.

그리고 여기서 말하는 `Tag`는 메시지의 무결성과 인증성을 확인하기 위한 값으로 보면 된다. 단순히 쿠키를 보내는 것만으로는 그 값이 중간에 변조되지 않았는지 확인하기 어렵기 때문에, 서버는 MAC이나 인증 태그 같은 값을 통해 메시지가 정상적인지 확인할 수 있다.

![](../images/Pasted%20image%2020260603225216.png)

정리하면, HTTP는 상태를 기억하지 않기 때문에 로그인 상태를 유지하려면 쿠키가 필요하다. 하지만 쿠키는 인증 정보와 직접 연결될 수 있기 때문에 보호가 중요하다. 그래서 HTTPS에서만 보내기 위한 `Secure`, JavaScript 접근을 막기 위한 `HttpOnly`, 다른 사이트 요청을 제한하기 위한 `SameSite` 같은 설정을 사용한다. 웹 보안은 결국 이런 상태 유지 정보가 탈취되거나 악용되지 않도록 막는 것에서 시작된다.

---
#  Web Security

HTTP의 기본 구조와 쿠키까지 보면, 이제 왜 웹 보안이 필요한지 자연스럽게 이어진다. 웹은 브라우저와 서버가 계속 데이터를 주고받는 구조이고, 로그인 상태나 사용자 입력값처럼 중요한 정보가 오가기 때문에 공격자가 끼어들 수 있는 지점이 많다. 그래서 웹 보안은 단순히 서버를 막는 것이 아니라, **웹 애플리케이션과 웹 서비스를 여러 위협으로부터 보호하는 전체 과정**이라고 볼 수 있다.

웹 보안에서 다루는 범위는 꽤 넓다. 먼저 개발 단계에서는 **secure coding practices**, 즉 안전한 코딩 습관이 중요하다. 예를 들어 사용자가 입력한 값을 그대로 SQL 문에 붙이거나, HTML에 그대로 출력하면 SQL Injection이나 XSS 같은 문제가 생길 수 있다. 그래서 처음부터 공격 가능성을 생각하면서 코드를 작성해야 한다.

그다음은 **입력값 검증**이다. 웹 애플리케이션은 사용자가 보내는 값을 신뢰하면 안 된다. 로그인 폼, 검색창, 게시글 작성, 파일 업로드, URL 파라미터처럼 사용자가 조작할 수 있는 모든 입력은 공격 벡터가 될 수 있다. 그래서 서버는 입력값의 형식, 길이, 허용 범위 등을 확인해야 하고, 필요한 경우 escape나 encoding도 해야 한다.

또 중요한 부분은 **세션 관리**다. 앞에서 본 것처럼 HTTP는 상태를 기억하지 못하기 때문에 쿠키나 세션을 이용해서 로그인 상태를 유지한다. 그런데 이 세션 정보가 탈취되거나 예측 가능하면 공격자가 사용자 계정을 대신 사용할 수 있다. 그래서 세션 ID는 충분히 랜덤해야 하고, 쿠키에는 `Secure`, `HttpOnly`, `SameSite` 같은 보호 설정을 붙여야 한다.

**인증 방식**도 웹 보안의 핵심이다. 사용자가 누구인지 확인하는 과정이 약하면, 비밀번호 탈취나 인증 우회가 발생할 수 있다. 단순 로그인뿐만 아니라 JWT, 세션 기반 인증, OAuth 같은 방식에서도 토큰 검증, 만료 시간, 발급자 확인, 권한 확인이 제대로 되어야 한다.

그리고 민감한 데이터는 **HTTPS 같은 암호화 프로토콜**을 통해 보호해야 한다. 로그인 정보, 쿠키, 결제 정보, 개인정보가 평문 HTTP로 전송되면 네트워크 중간에서 탈취될 수 있다. HTTPS는 TLS를 이용해서 클라이언트와 서버 사이의 통신을 암호화하고, 서버 인증과 데이터 무결성도 함께 제공한다.

---
# The 10 Web Application Security Risks

이런 웹 보안 위험을 체계적으로 정리한 대표적인 기준이 **OWASP Top 10**이다. OWASP는 웹 애플리케이션 보안을 개선하기 위한 비영리 단체이고, OWASP Top 10은 웹에서 가장 중요하고 자주 발생하는 보안 위험 10가지를 정리한 목록이다.

OWASP Top 10이 중요한 이유는 웹 보안에서 자주 나오는 문제가 완전히 새로운 문제가 아니라, 반복적으로 등장하는 유형이 많기 때문이다. 예를 들어 Injection, 인증 실패, 접근 제어 문제, 보안 설정 오류, 취약한 컴포넌트 사용 같은 문제는 실제 서비스에서도 계속 발생한다. 그래서 OWASP Top 10을 보면 “웹 애플리케이션에서 특히 조심해야 할 대표적인 위험이 무엇인지”를 큰 틀에서 파악할 수 있다.

![](../images/Pasted%20image%2020260603225644.png)

2021년과 2025년 목록을 비교하는 그림은 웹 보안 위험의 우선순위가 시간이 지나면서 바뀐다는 점을 보여준다. 예전부터 중요했던 Injection이나 보안 설정 오류도 계속 남아 있지만, 최근에는 소프트웨어 공급망 문제, 잘못된 인증, 로깅과 모니터링 실패 같은 항목도 중요하게 다뤄진다. 즉, 웹 보안은 단순히 코드 한 줄을 안전하게 쓰는 문제를 넘어서, 의존성 관리, 배포 설정, 인증 구조, 모니터링까지 포함하는 넓은 영역으로 확장되고 있다.

---
# Injection

OWASP Top 10에서 Injection을 먼저 다루는 이유는, 웹 애플리케이션이 결국 **사용자 입력을 받아서 서버 내부의 명령이나 쿼리로 처리하는 구조**이기 때문이다. 사용자가 입력한 값이 단순한 데이터로만 취급되어야 하는데, 그 값이 SQL 명령이나 JavaScript 코드처럼 실행 가능한 의미를 갖게 되면 Injection 문제가 생긴다.

Injection은 말 그대로 공격자가 악성 코드를 “주입”하는 공격이다. 대표적으로 SQL Injection과 XSS가 있다. 둘 다 사용자의 입력이 안전하게 처리되지 않았을 때 발생하지만, 실행되는 위치가 다르다. SQL Injection은 서버 쪽 데이터베이스에서 문제가 생기고, XSS는 피해자의 브라우저에서 악성 스크립트가 실행되는 쪽에 가깝다.

## SQL Injection

SQL Injection은 사용자가 입력한 값이 SQL 쿼리문 안에 그대로 붙으면서 발생한다. SQL은 데이터베이스에서 데이터를 조회, 삽입, 수정, 삭제하기 위해 사용하는 언어다. 예를 들어 다음과 같은 쿼리가 있다고 하자.

`SELECT * FROM member WHERE user_id = 'helloWorld';`

원래 의도는 `user_id`가 `helloWorld`인 회원만 조회하는 것이다. 그런데 사용자 입력값을 문자열로 단순히 이어 붙여서 SQL을 만들면, 공격자는 입력값 안에 SQL 문법을 섞어 넣을 수 있다.

예를 들어 로그인이나 검색 기능에서 사용자가 입력한 값이 그대로 쿼리에 들어간다고 하면, 공격자는 `' OR '1'='1` 같은 값을 넣을 수 있다. 그러면 원래는 특정 사용자만 찾는 조건이었는데, 뒤에 항상 참이 되는 조건이 붙는다.

`WHERE last_name = 'Smith' OR '1'='1'`

여기서 `'1'='1'`은 항상 참이다. 그래서 조건문 전체가 참이 되어버리고, 특정 사용자 하나만 조회해야 하는 상황에서 전체 사용자 정보가 조회될 수 있다. 실습 화면에서 여러 사용자의 정보가 한꺼번에 출력되는 이유가 바로 이 구조 때문이다.

이 공격의 핵심은 사용자의 입력이 “데이터”로 처리되지 않고, SQL 문법의 일부로 해석된다는 점이다. 서버는 공격자가 입력한 문자열을 그냥 이름이나 검색어로 봐야 하는데, 쿼리를 문자열로 조립해버리면 데이터와 명령의 경계가 무너진다.

SQL Injection은 단순 조회 우회에서 끝나지 않을 수 있다. 공격자가 입력값 안에 `update` 같은 SQL 문을 넣으면 데이터베이스의 값을 수정할 수도 있다. 예시에서는 다음과 같은 입력을 사용한다.

`'; update employees set salary=12251234 where last_name='Smith';--`

이 입력은 앞의 문자열 조건을 강제로 닫고, 새로운 `update` 문을 실행하게 만든다. 그 결과 `last_name`이 `Smith`인 직원의 salary 값이 공격자가 원하는 값으로 바뀐다. 마지막의 `--`는 SQL에서 주석 처리 역할을 해서, 뒤에 남은 쿼리 조각이 실행에 방해되지 않도록 만든다.

그래서 SQL Injection의 피해는 꽤 크다. 로그인 우회, 사용자 정보 유출, 데이터 조작, 데이터 삭제, 심하면 시스템 장악까지 이어질 수 있다. 단순히 “검색창에 이상한 값을 넣었다” 수준이 아니라, 데이터베이스가 공격자의 명령을 실행하게 되는 문제다.

원인은 결국 입력값 검증과 쿼리 생성 방식에 있다. 사용자가 입력한 값을 제대로 검사하지 않거나, SQL 문자열에 그대로 이어 붙이면 공격자가 원하는 SQL 문법을 삽입할 수 있다. 특히 로그인 폼, 검색 기능, 게시글 조회, 다운로드 기능, 관리자 페이지처럼 데이터베이스와 연결되는 입력 지점이 취약해질 수 있다.

---
## Mitigation

방어의 핵심은 **사용자 입력을 SQL 문자열에 직접 붙이지 않는 것**이다. 즉, 문자열 연결 방식으로 쿼리를 만들면 안 된다.

취약한 방식은 이런 구조다.

```java
String q = "SELECT * FROM users WHERE id = '" + uid + "'";
ResultSet rs = stmt.executeQuery(q);
```

여기서는 `uid`가 SQL 문장 안에 직접 들어간다. 공격자가 `uid`에 `' OR '1'='1`을 넣으면 최종 SQL 문장이 바뀌어버린다.

안전한 방식은 PreparedStatement 같은 **parameterized query**를 사용하는 것이다.

```java
PreparedStatement ps = conn.prepareStatement(
    "SELECT * FROM users WHERE id = ?"
);
ps.setString(1, uid);
ResultSet rs = ps.executeQuery();
```

이 방식에서는 SQL 코드와 사용자 입력 데이터가 분리된다. `?` 자리에 들어가는 값은 SQL 문법으로 해석되지 않고, 하나의 문자열 데이터로 처리된다. 그래서 사용자가 `' OR '1'='1`을 입력해도 그것은 조건문이 아니라 그냥 `id` 값으로 비교되는 문자열이 된다.

ORM을 사용하는 것도 도움이 된다. Django ORM, SQLAlchemy, JPA, MyBatis 같은 도구들은 보통 내부적으로 파라미터 바인딩을 사용하기 때문에 직접 문자열로 SQL을 조립하는 것보다 안전하다. 다만 raw SQL을 직접 쓰면 다시 위험해질 수 있으므로 주의해야 한다.

입력값 검증도 필요하지만, 이것만으로는 부족하다. 예를 들어 SQL 키워드를 막는 blocklist 방식은 우회하기 쉽다. 대신 입력값의 타입, 길이, 허용 문자 범위를 제한하는 allowlist 방식이 보조 방어로 더 적절하다. 하지만 가장 중요한 1차 방어는 여전히 parameterized query다.

---
## XSS (Cross Site Scripting) attack

SQL Injection을 본 다음 XSS를 보는 이유는, 둘 다 **사용자 입력이 코드처럼 해석될 때 발생하는 Injection 계열 공격**이기 때문이다. 차이는 SQL Injection은 입력이 서버의 데이터베이스 쿼리로 실행되는 문제이고, XSS는 입력이 피해자 브라우저에서 JavaScript로 실행되는 문제라는 점이다.

XSS는 Cross-Site Scripting의 약자다. 공격자가 게시글, 댓글, URL 파라미터, 입력 폼 등에 악성 스크립트를 넣고, 웹 애플리케이션이 그 값을 제대로 처리하지 않은 채 페이지에 출력하면 브라우저가 그것을 일반 텍스트가 아니라 JavaScript 코드로 실행하게 된다.

흐름은 이렇게 볼 수 있다.

![](../images/Pasted%20image%2020260603231441.png)

- 공격자가 악성 입력을 넣는다.  
- 웹 애플리케이션이 그 입력을 저장하거나 응답에 그대로 포함한다.  
- 피해자의 브라우저가 해당 페이지를 렌더링한다.  
- 브라우저가 삽입된 스크립트를 실행한다.

여기서 핵심은 브라우저가 “이 스크립트는 공격자가 넣은 것이다”라고 구분하지 못한다는 점이다. 웹사이트가 응답으로 보낸 HTML 안에 스크립트가 들어 있으면, 브라우저는 그것을 신뢰된 사이트의 코드처럼 실행한다. 그래서 XSS가 발생하면 쿠키 탈취, 가짜 로그인 폼 삽입, 사용자 대신 요청 보내기, 페이지 변조 같은 공격이 가능해진다.

XSS는 보통 사용자의 입력을 제대로 검증하거나 sanitize하지 않았을 때 발생한다. 특히 사용자의 입력값을 HTML 페이지에 다시 출력하면서 적절한 encoding을 하지 않으면 문제가 된다. 예를 들어 사용자가 입력한 이름을 `<div>Hello, 사용자이름</div>` 형태로 출력한다고 할 때, 사용자 이름 자리에 `<script>...</script>`가 들어가면 브라우저는 그것을 화면에 글자로 보여주는 것이 아니라 스크립트로 실행할 수 있다.

XSS는 크게 Stored XSS, Reflected XSS, DOM-based XSS로 나눌 수 있다.

### Stored XSS
Stored XSS는 악성 스크립트가 서버에 저장되는 방식이다. 예를 들어 공격자가 댓글에 악성 JavaScript를 작성했는데, 서버가 그 댓글을 그대로 DB에 저장한다고 하자. 이후 다른 사용자가 해당 게시글을 보면 서버는 저장된 댓글을 페이지에 포함해서 보내고, 피해자의 브라우저에서 악성 스크립트가 자동 실행된다.

Stored XSS가 위험한 이유는 공격자가 매번 피해자에게 특수한 URL을 보내지 않아도 된다는 점이다. 악성 코드가 서버에 저장되어 있기 때문에, 해당 페이지를 보는 사용자들은 계속 영향을 받을 수 있다. 게시판 댓글, 프로필 소개, 상품 리뷰, 이미지 설명 같은 사용자 작성 콘텐츠에서 특히 문제가 될 수 있다.

![](../images/Pasted%20image%2020260603231745.png)

예시에서도 댓글 입력란에 단순한 글이 아니라 JavaScript payload를 넣고 있다. 웹 애플리케이션이 이 값을 안전하게 처리하지 않으면, 이후 페이지가 로드될 때 댓글 안의 스크립트가 실행된다. 개발자 도구나 콘솔을 보면 실제로 스크립트 실행으로 인해 특정 함수가 호출되거나 값이 출력되는 것을 확인할 수 있다.

---
### Reflected XSS

Reflected XSS는 악성 입력이 서버에 저장되지는 않고, 요청에 포함된 값이 바로 응답에 반사되어 실행되는 방식이다. 예를 들어 검색 페이지에서 사용자가 입력한 검색어를 그대로 “검색어: xxx”처럼 화면에 출력한다고 하자. 공격자가 URL 파라미터에 `<script>alert('XSS')</script>` 같은 값을 넣고 피해자에게 그 링크를 보내면, 피해자가 링크를 여는 순간 서버 응답에 그 값이 포함되고 브라우저에서 실행될 수 있다.

Stored XSS와 Reflected XSS의 차이는 지속성이다. Stored XSS는 악성 스크립트가 서버에 저장되어 계속 실행될 수 있고, Reflected XSS는 특정 요청에 포함된 값이 즉시 응답에 반영될 때 실행된다. 그래서 Reflected XSS는 보통 공격자가 피해자에게 조작된 링크를 클릭하게 만드는 방식과 연결된다.

DOM-based XSS는 서버 응답보다는 브라우저 내부의 JavaScript 처리 과정에서 발생한다. 서버가 직접 악성 스크립트를 내려주지 않더라도, 클라이언트 측 JavaScript가 `location`, `hash`, `document.URL` 같은 값을 읽어서 DOM에 그대로 삽입하면 브라우저 안에서 스크립트가 실행될 수 있다. 즉, 취약점의 중심이 서버 템플릿이 아니라 프론트엔드 코드에 있는 경우다.

XSS를 막는 가장 기본적인 방법은 사용자의 입력을 HTML에 그대로 넣지 않는 것이다. 출력 위치에 맞게 encoding해야 한다. HTML 본문에 출력할 때는 `<`, `>`, `&` 같은 문자를 HTML entity로 바꿔야 하고, URL에 넣을 때는 URL encoding을 해야 한다. 중요한 것은 “입력값을 한 번 검사하면 끝”이 아니라, **출력되는 문맥에 맞게 escaping해야 한다**는 점이다.

예를 들어 취약한 코드는 이런 식이다.

```html
<div>Hello, ${userName}</div>
```

여기서 `userName`에 `<script>...</script>`가 들어오면 그대로 HTML에 삽입되어 실행될 수 있다.

---
### Mitigation

반대로 안전한 템플릿이나 React 같은 방식에서는 사용자 입력을 기본적으로 텍스트로 escape해서 출력한다.

```html
<div>Hello, {{ userName }}</div>
```

또는 React에서는 다음처럼 작성하면 기본적으로 텍스트로 처리된다.

```jsx
<div>{userName}</div>
```

이렇게 하면 `<script>`가 실제 태그로 해석되는 것이 아니라 `&lt;script&gt;`처럼 문자로 표시된다.

추가 방어로는 `HttpOnly` 쿠키와 CSP가 있다. `HttpOnly`를 설정하면 JavaScript가 `document.cookie`로 세션 쿠키를 읽을 수 없기 때문에, XSS가 발생하더라도 쿠키 탈취 피해를 줄일 수 있다. 다만 XSS 자체를 막는 것은 아니고 피해를 줄이는 방어라고 봐야 한다.

CSP, 즉 Content Security Policy는 브라우저에게 어떤 스크립트를 실행해도 되는지 제한하는 정책이다. 예를 들어 `script-src 'self'`를 설정하면 외부 출처의 스크립트 실행을 제한할 수 있고, nonce를 사용하면 허용된 스크립트만 실행되도록 만들 수 있다. 이것도 XSS 방어에서 중요한 보조 장치다.

정리하면, XSS는 사용자의 입력이 웹 페이지 안에서 JavaScript로 실행되는 취약점이다. SQL Injection이 데이터베이스를 공격한다면, XSS는 피해자의 브라우저를 공격한다. Stored XSS는 악성 스크립트가 서버에 저장되어 여러 사용자에게 반복적으로 실행되는 방식이고, Reflected XSS는 요청에 포함된 값이 즉시 응답에 반영되어 실행되는 방식이다. 방어의 핵심은 사용자 입력을 그대로 출력하지 않고, 문맥에 맞게 escaping하며, `HttpOnly` 쿠키와 CSP 같은 보호 장치를 함께 적용하는 것이다.

---
# Token based Auth: JSON Web Token (JWT)

XSS까지 봤기 때문에, 이제 토큰 기반 인증을 다루는 이유는 자연스럽다. 웹에서는 로그인 상태를 유지해야 하고, 그 상태 정보가 쿠키나 토큰으로 오가는데, XSS 같은 공격이 발생하면 이 인증 정보가 탈취될 수 있다. 그래서 JWT를 단순히 “로그인 토큰”으로만 보면 안 되고, **무엇이 보호되고 무엇이 보호되지 않는지**를 정확히 알아야 한다.

JWT는 JSON Web Token의 약자이고, 서버와 클라이언트 사이에서 사용자 인증 정보를 전달할 때 자주 사용된다. 보통 로그인에 성공하면 서버가 JWT를 발급하고, 클라이언트는 이후 요청마다 이 토큰을 함께 보낸다. 서버는 토큰을 보고 “이 사용자가 누구인지”, “토큰이 유효한지”를 판단한다.

JWT는 크게 세 부분으로 구성된다.

![](../images/Pasted%20image%2020260603232106.png)

Header에는 토큰의 타입과 서명 알고리즘 정보가 들어간다. 예를 들어 `alg: HS256`, `typ: JWT` 같은 값이 들어간다. 여기서 `alg`는 어떤 알고리즘으로 서명했는지를 나타낸다.

Payload에는 실제 claim 정보가 들어간다. 예를 들어 `sub`는 사용자 식별자, `iss`는 토큰 발급자, `exp`는 만료 시간, `iat`는 발급 시간을 의미한다. 중요한 점은 payload는 **암호화된 것이 아니라 Base64url로 인코딩된 것**이라는 점이다. 그래서 누구나 JWT를 디코딩하면 payload 내용을 읽을 수 있다.

Signature는 Header와 Payload가 변조되지 않았는지 확인하기 위한 값이다. 예를 들어 HS256 방식에서는 Header와 Payload를 합친 뒤, 서버만 알고 있는 secret key로 HMAC을 계산한다. 서버는 나중에 요청으로 들어온 JWT에 대해 다시 서명을 계산해보고, 토큰의 Signature와 일치하는지 확인한다. 일치하면 “이 토큰은 서버가 발급한 뒤 변조되지 않았다”고 판단할 수 있다.

여기서 가장 중요한 정리는 이거다.

>**JWT는 signed token이지 encrypted token이 아니다.**

즉, JWT는 변조 여부를 확인할 수 있지만, 내용이 숨겨지는 것은 아니다. Payload에 비밀번호, 주민번호, 카드번호 같은 민감한 정보를 넣으면 안 된다. 사용자는 물론이고, 중간에서 토큰을 본 사람도 payload 자체는 디코딩해서 읽을 수 있기 때문이다.

JWT 검증에서는 서명만 확인하면 부족하다. 반드시 **Signature와 Claim을 둘 다 검증**해야 한다.

Signature 검증은 “이 토큰이 서버가 발급한 것이 맞는가”, “중간에 payload가 바뀌지 않았는가”를 확인하는 과정이다. 공격자가 payload의 `sub`를 `alice`에서 `admin`으로 바꿔도, secret key를 모르기 때문에 올바른 signature를 만들 수 없다. 그래서 서버가 signature를 검증하면 변조된 토큰을 거부할 수 있다.

하지만 signature가 맞아도 claim 검증을 안 하면 문제가 생긴다. 예를 들어 `exp`를 확인하지 않으면 만료된 토큰이 계속 사용될 수 있다. `iss`를 확인하지 않으면 신뢰하지 않는 서버가 발급한 토큰을 받아들일 수 있다. `aud`를 확인하지 않으면 다른 서비스용으로 발급된 토큰이 현재 서비스에서 사용될 수 있다.

그래서 JWT 검증에서는 최소한 다음을 확인해야 한다.

- `exp`: 토큰이 만료되지 않았는지  
- `iss`: 신뢰하는 발급자가 발급했는지  
- `aud`: 현재 서비스에 사용되는 토큰이 맞는지  
- `nbf`: 사용 가능 시점 이전에 사용된 것은 아닌지  
- `alg`: 서버가 허용한 알고리즘인지

JWT에서 실제로 많이 발생했던 취약점도 이 흐름과 연결된다.

첫 번째는 `alg: none` 문제다. 예전 일부 라이브러리는 header에 `alg: none`이 들어오면 서명이 없는 토큰도 받아들였다. 그러면 공격자가 payload를 마음대로 만들고 signature 없이 제출해도 인증이 통과될 수 있다. 그래서 서버에서는 클라이언트가 보낸 `alg` 값을 그대로 믿으면 안 되고, 서버 쪽에서 허용할 알고리즘을 고정해야 한다.

두 번째는 알고리즘 혼동 공격이다. 예를 들어 원래 서버가 RS256을 사용한다고 하자. RS256은 개인키로 서명하고 공개키로 검증하는 방식이다. 그런데 서버가 HS256도 허용하고, 검증 과정에서 공개키를 HMAC secret처럼 사용해버리면 문제가 생긴다. 공격자는 공개키를 알고 있으므로, 그 공개키를 secret처럼 사용해서 HS256 토큰을 위조할 수 있다. 그래서 알고리즘은 반드시 서버에서 명확히 제한해야 한다.

세 번째는 claim 검증 누락이다. 서명이 맞는 토큰이라도 만료 시간이 지났거나, 발급자가 다르거나, 대상 서비스가 다르면 거부해야 한다. 그런데 서버가 단순히 signature만 보고 통과시키면, 오래된 토큰이나 다른 용도의 토큰이 계속 사용될 수 있다.

네 번째는 JWT 저장 위치 문제다. JWT를 `localStorage`에 저장하면 JavaScript로 쉽게 접근할 수 있다. 그래서 XSS가 발생했을 때 공격 스크립트가 `localStorage`에서 토큰을 읽어 외부로 보낼 수 있다. 이런 이유로 세션 토큰은 가능하면 `HttpOnly` 쿠키에 저장하거나, 메모리 저장 방식을 고려하는 것이 더 안전하다.

다만 `HttpOnly` 쿠키를 쓰면 JavaScript가 토큰을 읽지 못한다는 장점이 있지만, 브라우저가 요청에 쿠키를 자동으로 붙이기 때문에 CSRF 방어도 같이 고려해야 한다. 그래서 `SameSite`, CSRF token, Origin 검증 같은 방어가 함께 필요할 수 있다.

---
# SSRF (Server Side Request Forgery)

웹 보안에서 중요한 공격 대상이 항상 “사용자 브라우저”나 “DB”만은 아니다. 서버도 다른 서버나 내부 시스템으로 요청을 보낼 수 있는데, 이 기능이 잘못 열려 있으면 공격자가 **서버를 대신 이용해서 접근하면 안 되는 내부 주소에 요청을 보내게 만들 수 있다.**

SSRF는 Server-Side Request Forgery의 약자다. 말 그대로 서버 측 요청을 위조하는 공격이다. 공격자가 직접 접근할 수 없는 내부 서비스가 있을 때, 외부에 공개된 웹 서버를 중간에 이용해서 그 내부 서비스로 요청을 보내게 만드는 방식이다.

예를 들어 웹 애플리케이션에 “URL을 입력하면 해당 이미지를 가져와서 미리보기 해주는 기능”이 있다고 하자. 원래 의도는 사용자가 입력한 이미지 URL을 서버가 가져와서 보여주는 것이다. 그런데 서버가 사용자가 입력한 URL을 그대로 fetch하면, 공격자는 외부 이미지 주소 대신 내부 주소를 넣을 수 있다.

예를 들면 이런 식이다.

`http://169.254.169.254/latest/meta-data/...`

이 주소는 클라우드 환경에서 인스턴스 메타데이터를 가져올 때 사용되는 주소로 자주 언급된다. 공격자가 자기 컴퓨터에서 이 주소에 직접 접근할 수는 없지만, 클라우드 내부에 있는 서버는 접근할 수 있을 수 있다. 그래서 서버가 이 URL을 대신 요청하게 만들면, 공격자는 서버의 권한과 네트워크 위치를 이용해서 민감한 정보를 얻을 수 있다.

SSRF의 핵심은 **서버가 공격자의 프록시처럼 사용된다**는 점이다. 공격자는 외부에서 막혀 있는 내부망, 관리자 페이지, 메타데이터 서버, 로컬호스트 서비스에 직접 접근하지 못하지만, 서버는 내부 네트워크에 있기 때문에 접근할 수 있다. 그래서 서버에게 “이 URL 좀 가져와줘”라고 시키는 구조가 위험해진다.

## Common SSRF attack partterns

공격 패턴은 여러 가지가 있다.
#### Cloud metadata
Cloud metadata 공격은 클라우드 메타데이터 서버를 노리는 방식이다. 잘못하면 임시 인증 정보, 인스턴스 정보, IAM 관련 정보가 노출될 수 있다.

#### URL Parser Confusion
URL parser confusion은 URL 해석 차이를 이용하는 방식이다. 예를 들어 `http://expected.com@internal-host/` 같은 URL은 사람이 보기에는 `expected.com`처럼 보일 수 있지만, 실제 접속 대상은 `internal-host`로 해석될 수 있다. 또는 `expected.com.evil.com`처럼 도메인 접미사를 교묘하게 붙여 allowlist 검사를 우회하려고 할 수도 있다.

#### Protocol abuse
Protocol abuse는 HTTP가 아닌 다른 scheme을 이용하는 방식이다. 예를 들어 `file:///etc/passwd` 같은 값을 넣어서 서버 로컬 파일을 읽게 만들려는 시도다. 서버 코드가 다양한 프로토콜을 허용하면 이런 문제가 생길 수 있다.

#### IP encoding trick
IP encoding trick은 IP 주소를 다른 표현으로 바꿔 우회하는 방식이다. 예를 들어 `127.0.0.1`을 십진수로 표현해서 `2130706433`처럼 넣는 경우다. 단순히 문자열에 `127.0.0.1`이 있는지만 검사하면 이런 우회를 막지 못한다.

#### DNS rebinding
DNS rebinding은 처음 DNS를 확인할 때는 정상 외부 IP를 가리키다가, 실제 접속 시점에는 내부 IP로 바뀌게 만드는 방식이다. 즉, DNS resolve 시점과 HTTP connect 시점 사이의 차이를 이용한다. 그래서 도메인만 한 번 검사하고 끝내면 위험할 수 있다.

----
## Defenses

방어의 핵심은 사용자가 제공한 URL을 서버가 그대로 요청하지 않게 하는 것이다. 가장 안전한 방식은 애초에 서버 측 fetch 대상 URL을 사용자에게 자유롭게 입력받지 않는 것이다. 꼭 필요하다면 blocklist가 아니라 **allowlist**를 사용해야 한다.

blocklist는 `localhost`, `127.0.0.1`, `169.254.169.254` 같은 위험한 주소만 막는 방식인데, 우회 표현이 너무 많다. 반대로 allowlist는 `example-cdn.com`, `trusted-api.com`처럼 허용된 목적지만 요청하도록 제한한다. 웹 보안에서는 보통 “막을 것만 정하기”보다 “허용할 것만 정하기”가 더 안전하다.

또한 private IP와 link-local 주소를 차단해야 한다. 예를 들어 `10.0.0.0/8`, `127.0.0.1`, `::1`, `169.254.0.0/16` 같은 내부망·로컬·메타데이터 관련 주소는 서버 측 요청 대상에서 제외해야 한다.

네트워크 레벨에서도 egress filtering이 필요하다. 애플리케이션 코드에서 한 번 막는 것만으로는 부족할 수 있으므로, 방화벽이나 보안 그룹에서 서버가 외부 또는 내부의 어떤 주소로 나갈 수 있는지 제한해야 한다.

AWS 같은 클라우드 환경에서는 IMDSv2처럼 세션 토큰이 필요한 메타데이터 접근 방식을 사용하는 것이 좋다. IMDSv2는 단순 GET 요청만으로 메타데이터를 가져오는 것을 어렵게 만들어 SSRF 피해를 줄인다.

정리하면, SSRF는 공격자가 서버에게 특정 URL을 요청하게 만들어, 원래 공격자가 직접 접근할 수 없는 내부 자원에 접근하는 공격이다. 위험한 이유는 서버가 내부망에 위치하고 더 많은 권한을 가질 수 있기 때문이다. 따라서 사용자 입력 URL을 그대로 fetch하지 않고, allowlist 기반으로 목적지를 제한하며, 내부 IP와 link-local 주소를 차단하고, 네트워크 egress 정책까지 함께 적용해야 한다.

---
# CSRF (Cross-Site Request Forgery)

CSRF는 Cross-Site Request Forgery의 약자다. 핵심은 사용자가 어떤 사이트에 로그인해 있는 상태를 공격자가 악용한다는 것이다. 브라우저는 특정 사이트에 요청을 보낼 때, 그 사이트의 쿠키를 자동으로 붙여서 보낸다. 이 특징 때문에 문제가 생긴다.

![](../images/Pasted%20image%2020260603232921.png)

예를 들어 사용자가 `bank.com`에 로그인하면, 서버는 브라우저에 다음과 같은 세션 쿠키를 내려준다.

`Set-Cookie: session=ABC`

이후 사용자가 `bank.com`에 요청을 보낼 때 브라우저는 자동으로 `session=ABC` 쿠키를 함께 보낸다. 서버는 이 쿠키를 보고 “로그인한 사용자구나”라고 판단한다.

문제는 사용자가 다른 탭에서 `evil.com`에 접속했을 때도 발생할 수 있다. `evil.com` 페이지 안에 `bank.com/transfer`로 자동 제출되는 form이 숨어 있다고 하자. 사용자가 버튼을 누르지 않아도 JavaScript나 자동 submit을 통해 `bank.com`으로 POST 요청이 전송될 수 있다.

이때 요청을 시작한 페이지는 `evil.com`이지만, 요청 대상은 `bank.com`이다. 그런데 브라우저는 `bank.com`으로 요청을 보내는 것이기 때문에, 저장해둔 `bank.com`의 쿠키를 자동으로 붙인다.

즉, 공격자는 사용자의 세션 쿠키 값을 직접 알 필요가 없다. 브라우저가 알아서 쿠키를 붙여주기 때문이다. 서버 입장에서는 정상 사용자의 브라우저에서 온 요청처럼 보일 수 있고, 그래서 송금, 이메일 변경, 비밀번호 변경 같은 요청이 실행될 수 있다.

CSRF의 핵심 흐름은 이렇게 정리할 수 있다.

- 사용자가 `bank.com`에 로그인한다.  
- 브라우저에 `session=ABC` 같은 쿠키가 저장된다.  
- 사용자가 다른 탭에서 `evil.com`에 접속한다.  
- `evil.com`이 `bank.com`으로 요청을 보내게 만든다.  
- 브라우저는 `bank.com` 쿠키를 자동으로 붙여 보낸다.  
- `bank.com` 서버가 요청을 정상 요청으로 착각하면 공격이 성공한다.

여기서 XSS와 헷갈릴 수 있는데, XSS는 공격자의 스크립트가 피해자의 브라우저 안에서 실행되는 문제다. 반면 CSRF는 공격자가 피해자의 쿠키를 읽는 것이 아니라, **브라우저가 쿠키를 자동으로 붙여 보내는 특성**을 이용하는 문제다. 그래서 `HttpOnly`로 JavaScript가 쿠키를 못 읽게 해도 CSRF는 별도로 막아야 한다.

---
## Defenses

### SameSite cookie
방어 방법으로는 먼저 `SameSite` 쿠키 설정이 있다. `SameSite`는 다른 사이트에서 시작된 요청에 쿠키를 붙일지 제한하는 옵션이다.

`SameSite=Strict`는 cross-site 요청에는 쿠키를 거의 보내지 않는다. 보안은 강하지만 사용자 경험에 영향을 줄 수 있다.

`SameSite=Lax`는 일반적인 페이지 이동 같은 안전한 GET 요청에는 쿠키를 보낼 수 있지만, 외부 사이트에서 시작된 위험한 POST 요청에는 쿠키 전송을 제한한다. 실무에서 기본값으로 자주 사용된다.

`SameSite=None`은 cross-site 요청에도 쿠키를 보내겠다는 의미다. 이 경우 반드시 `Secure`와 함께 사용해야 하고, 정말 필요한 경우에만 써야 한다.

### CSRF token
또 다른 대표 방어는 **CSRF token**이다. 서버가 form을 내려줄 때 예측할 수 없는 랜덤 토큰을 함께 넣어두고, 사용자가 POST 요청을 보낼 때 그 토큰도 같이 제출하게 한다. 서버는 요청을 처리하기 전에 세션에 저장된 토큰과 제출된 토큰이 일치하는지 확인한다.

공격자는 `evil.com`에서 `bank.com`으로 요청을 보내게 만들 수는 있지만, 정상 페이지에 포함된 CSRF token 값을 알 수 없기 때문에 올바른 토큰을 함께 보낼 수 없다. 그래서 서버는 토큰이 없거나 틀린 요청을 거부할 수 있다.

정리하면, CSRF는 사용자가 로그인된 상태에서 공격자가 사용자의 브라우저를 이용해 원하지 않는 요청을 보내게 만드는 공격이다. 브라우저가 쿠키를 자동으로 붙여 보내기 때문에 발생하며, 공격자는 쿠키 값을 몰라도 공격을 시도할 수 있다. 방어하려면 `SameSite` 쿠키로 cross-site 쿠키 전송을 제한하고, 중요한 상태 변경 요청에는 CSRF token을 넣어서 요청이 정상 페이지에서 출발했는지 확인해야 한다.